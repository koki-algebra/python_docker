[tool.poetry]
name = "python-docker"
version = "0.1.0"
description = ""
authors = ["koki-algebra"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^1.26.2"
jupyterlab = "^4.0.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
